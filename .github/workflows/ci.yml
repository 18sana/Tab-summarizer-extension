name: Project CI

on:
  push:
  pull_request:

jobs:
  ai-monitor:
    uses: 18sana/ai-cicd-template/.github/workflows/ai-cicd-monitor.yml@main
    with:
      working-directory: .
    secrets: inherit
